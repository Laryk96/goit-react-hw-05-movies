const { default: styled } = require('@emotion/styled');

const MainTitle = styled.h1`
  font-size: 40px;
  margin-bottom: 30px;
  text-transform: uppercase;
  text-align: center;
`;

export { MainTitle };
